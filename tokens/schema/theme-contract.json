{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ZUI Theme Contract",
  "description": "Defines what a consumer theme MUST provide to use the zui design system.",
  "type": "object",
  "required": ["name", "mode", "colors", "ink"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Human-readable theme name (e.g. 'Ocean Light', 'Forest Dark')"
    },
    "mode": {
      "type": "string",
      "enum": ["light", "dark"],
      "description": "Whether this is a light or dark theme. Controls which shade maps to which semantic token."
    },
    "colors": {
      "type": "object",
      "description": "Color seeds for each intent. Each seed is { hue, chroma }.",
      "required": ["primary", "danger", "neutral"],
      "properties": {
        "primary": { "$ref": "#/$defs/colorSeed" },
        "danger": { "$ref": "#/$defs/colorSeed" },
        "info": { "$ref": "#/$defs/colorSeed" },
        "success": { "$ref": "#/$defs/colorSeed" },
        "warning": { "$ref": "#/$defs/colorSeed" },
        "neutral": { "$ref": "#/$defs/colorSeed" }
      },
      "additionalProperties": { "$ref": "#/$defs/colorSeed" }
    },
    "ink": {
      "type": "object",
      "description": "The two ink colors used for text-on-color. The system auto-selects which to use based on background lightness.",
      "required": ["dark", "light"],
      "properties": {
        "dark": {
          "type": "string",
          "description": "Dark ink color for text on light backgrounds (e.g. 'oklch(0.15 0.02 220)')"
        },
        "light": {
          "type": "string",
          "description": "Light ink color for text on dark backgrounds (e.g. 'oklch(0.98 0.005 220)')"
        }
      }
    },
    "lightnessCurve": {
      "type": "object",
      "description": "Optional override for the default lightness curve. Keys are shade names (50-950), values are OKLCH lightness (0-1).",
      "patternProperties": {
        "^(50|100|200|300|400|500|600|700|800|900|950)$": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        }
      },
      "additionalProperties": false
    }
  },
  "$defs": {
    "colorSeed": {
      "type": "object",
      "required": ["hue", "chroma"],
      "properties": {
        "hue": {
          "type": "number",
          "minimum": 0,
          "maximum": 360,
          "description": "OKLCH hue angle (0-360)"
        },
        "chroma": {
          "type": "number",
          "minimum": 0,
          "maximum": 0.4,
          "description": "OKLCH chroma (0 = gray, ~0.15 = vivid, ~0.3 = extremely saturated)"
        }
      },
      "additionalProperties": false
    }
  }
}
