import { tv } from 'tailwind-variants'

export const toolbarVariants = tv({
  base: [
    'flex flex-wrap items-center gap-toolbar-gap border-b border-border bg-card',
    'px-toolbar-x py-toolbar-y',
  ],
})
